import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.faisal.ipcalc', // Masukkan ID unik lo di sini
  appName: 'Kalkulator IP Address',    // Masukkan Nama Aplikasi lo di sini
  webDir: 'www',               // Pastikan ini tetap 'www'
};

export default config;